facebook4s {
  test {
    access-token = "access token for tests"
    ad-id = "an ad id you can get insights for"
    profile-name = "name of the 'me' profile the access token belongs to"
  }
}

include "test.overrides"
